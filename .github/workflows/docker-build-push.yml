# # TODO(폴리레포 전환 — 후속 작업):
# #   - ApiGatewayApplicationTests(@SpringBootTest 컨텍스트 로드 테스트)가 CI에 아직 안 붙어있다.
# #     폴리레포 전환 계획서 6장 2단계(과도기)에서는 groovy-infra가 아직 없어 로컬/CI 통합 테스트가
# #     원본 Groovy 레포의 docker-compose.local.yml에 의존하는 상태이므로, 이 서비스 레포 CI에서
# #     테스트를 어떻게 돌릴지(서비스 컨테이너 도입 vs 계속 보류)는 그 정책이 정해진 뒤 채운다.
# #   - 이 서비스는 event-contract를 의존하지 않으므로 계획서 3장(event-contract 스키마 검증)은
# #     해당 사항 없음 — notification-service 워크플로우의 contract-test job과 대칭되는 항목이
# #     여기 없는 이유.
# name: Gateway Build and Push
# 
# on:
#   push:
#     branches:
#       - main
# 
#   workflow_dispatch:
# 
# permissions:
#   contents: read
# 
# jobs:
#   gateway:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v4
# 
#       - name: Set up Docker Buildx
#         uses: docker/setup-buildx-action@v3
# 
#       - name: Log in to Docker Hub
#         uses: docker/login-action@v3
#         with:
#           username: ${{ secrets.DOCKERHUB_USERNAME }}
#           password: ${{ secrets.DOCKERHUB_TOKEN }}
# 
#       - name: Build and push gateway image
#         uses: docker/build-push-action@v6
#         with:
#           context: .
#           file: ./Dockerfile
#           push: true
#           tags: |
#             bebeghi/groovy-gateway-service:latest
#             bebeghi/groovy-gateway-service:${{ github.sha }}
#           cache-from: type=gha,scope=gateway
#           cache-to: type=gha,mode=max,scope=gateway
